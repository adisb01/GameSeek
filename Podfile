# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'iOS-final-app' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for iOS-final-app
	pod 'Firebase/Analytics'
	pod 'Firebase/Database'
	pod 'Firebase/Core'
	pod 'FirebaseUI'
	pod 'FirebaseUI/Auth'
  target 'iOS-final-appTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'iOS-final-appUITests' do
    # Pods for testing
  end

end
